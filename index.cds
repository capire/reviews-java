// This is for embedding reviews service + app in composite apps,
// hence we include everything: app service + api service.
using from './srv/reviews-service';
using from './srv/reviews-api';
namespace sap.capire.reviews;
